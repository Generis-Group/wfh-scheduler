import { ReferenceRouteLoading } from "@/components/reports/route-loading";

export default function Loading() {
  return (
    <ReferenceRouteLoading
      active="settings"
      kind="settings"
      variant="neutral"
    />
  );
}
